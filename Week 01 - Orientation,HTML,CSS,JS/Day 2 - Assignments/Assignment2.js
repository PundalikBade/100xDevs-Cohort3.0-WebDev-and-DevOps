// Write a function sum that finds the sum of two numbers. Side quest - Try passing in a instead of a number and see what happens?

function addNumber(a,b)
{
	let sum  = a + b;
	return sum;
}

let massage = addNumber("5",10);
console.log(massage);